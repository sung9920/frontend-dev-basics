/*
    Unnamed Export
    1. default 키워드
    2. 이름없이 export를 하기 때문에 하나만 export를 할 수 있다.
*/


// const add = function(a, b) {
//    return a + b;
// }
// export default add;

export default (a, b) => a + b;